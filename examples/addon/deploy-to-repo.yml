name: Deploy to repository.silvo

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  deploy:
    if: github.repository == 'OWNER/REPO'
    uses: OWNER/workflows/.github/workflows/deploy-to-repo.yml@v1
    with:
      channel: piers
    secrets: inherit
