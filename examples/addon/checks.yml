name: Code checks

on:
  push:
    branches: [main]
    paths-ignore: ['**/*.po']
  pull_request:
    branches: [main]
    paths-ignore: ['**/*.po']

jobs:
  checks:
    if: github.repository == 'OWNER/REPO'
    uses: OWNER/workflows/.github/workflows/checks.yml@v1
